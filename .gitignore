# ========== Unity Generated ==========
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
UserSettings/

# Generated by Unity (cache, player prefs, etc.)
MemoryCaptures/

# Asset import caches
Assets/AssetStore-5.x/
Assets/Plugins/Editor/**/Plugins/**/ # 一部プラグインの生成物（必要なら調整）

# ========== OS / IDE ==========
# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# Linux
*~ 
.#*

# Visual Studio
.vs/
*.csproj
*.sln
*.suo
*.user
*.userprefs
*.unityproj
*.pidb
*.booproj
*.svd

# Rider / IntelliJ
.idea/
*.sln.iml
*.ipr
*.iws

# VSCode
.vscode/

# ========== Build artifacts ==========
*.apk
*.aab
*.xcodeproj/
*.xcworkspace/
*.xcuserdata/
*.app
*.ipa
*.exe
*.dll
*.so
*.dylib

# ========== Unity specific caches / temp ==========
sysinfo.txt
CrashReports/

# Shader cache / compiled data
Library/ShaderCache/
Library/ScriptAssemblies/
Library/PackageCache/

# Addressables / AddressableAssetsData build results
Assets/AddressableAssetsData/*/*/*/ComputeHashes/*.bin
Assets/AddressableAssetsData/aa_config.json
Assets/AddressableAssetsData/*/*.bin
# （必要に応じてさらに細かく除外）

# ========== User-specific files ==========
# Unity scene/user prefs per-developer
Assets/**/*.csproj
Assets/**/*.unityproj
*.userprefs

# ========== Package Manager / Cache ==========
# npm / yarn (if used in project)
node_modules/

# Unity Package Manager cache (optional: exclude if you want to keep packages locally)
# Library/PackageCache/   # 通常は Library/ を gitignore しているので重複するが念のため

# ========== Large binary / binary metadata ==========
# もし大きなアセットを扱うなら Git LFS の利用を検討してください
# 例: .gitattributes に以下を追加
# *.psd filter=lfs diff=lfs merge=lfs -text
# Assets/**/*.fbx filter=lfs diff=lfs merge=lfs -text
# Assets/**/*.png filter=lfs diff=lfs merge=lfs -text

# ========== Custom (例) ==========
# ビルド出力先をプロジェクト直下にしている場合は除外
/Build/
/Builds/

# ========== Keep track of important project files ==========
# 以下は通常はコミットする（プロジェクト再現に必要）
!Assets/**/Editor/**   # エディタ拡張はコミットする
!ProjectSettings/**

# （必要なら下の行を外して調整）
# !/Assets/
# !/ProjectSettings/